# EnvTrap Docs

Mintlify documentation portal for EnvTrap.

## Local Development

To run the documentation portal locally, install the Mintlify CLI globally:

```bash
npm install -g mintlify
```

Start the local development server from the repository root:

```bash
mintlify dev
```
